# Core ML / embeddings
sentence-transformers>=2.2.0,<3.0
torch>=2.0.0
transformers>=4.30.0
numpy>=1.24.0

# Vector DB - new Chroma API (0.4+ uses PersistentClient, no duckdb+parquet)
chromadb>=0.4.24,<0.6.0 

# Pydantic v2 (required by chromadb>=0.4)
pydantic>=2.0.0,<3.0.0

# Document parsing
pdfplumber>=0.7.6
beautifulsoup4>=4.10.0

# LangChain (text splitting only — optional orchestration)
langchain>=0.1.0,<0.4.0
langchain-community>=0.0.20,<0.4.0
tiktoken>=0.5.0

# Web frameworks
flask>=2.3.0
gunicorn>=21.0.0
streamlit>=1.30.0

# LLM API - OpenRouter via OpenAI-compatible SDK
openai>=1.10.0

# HTTP client (used for OpenRouter calls)
httpx>=0.25.0

# Utilities
python-dotenv>=1.0.0
tqdm>=4.60.0
pandas>=2.0.0

# Testing
pytest>=7.0.0